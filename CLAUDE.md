# 强制规则
- 行动前先思考。写代码前先读已有文件。
- 输出力求简洁，思考力求充分。
- 优先编辑而不是整文件重写。
- 已读过且未变更的文件不重复读取。
- 声明完成前自测。
- 不写讨好性开场白与结尾废话。
- 方案保持简单直接。
- 用户指令始终高于本文件。

# 仓库指引
- `AGENTS.md` 是仓库规则、工作流与目录地图的唯一权威。
- 先读 `AGENTS.md`，再依其指向阅读相关文档。
- 本文件保持轻量，不重复仓库专属规则。
- 任何修改仓库文件的任务必须走完 crawler-workflow：**分支 → 实现 → PR**。邮件通知本期不接入。

# xiniu-crawler 项目说明
本仓库使用 `单仓 + 文档先行 + 轻量 DDD + 规格驱动开发（SDD）` 模型。
默认技术栈：**Python + uv**。生产元数据走外部 PolarDB，开发测试用 SQLite，
原始页与附件存阿里云 OSS。

文档统一使用中文撰写。
